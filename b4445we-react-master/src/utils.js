// JS extension because this will contain only JS Codes


// exporting multiple functions or variable
// can be imported only with exact names or using alias

export const questionUser = (question) => {
  alert(question);
}

export const inputFromUsr = () => {
  prompt('Enter soemthing...');
}

export default inputFromUsr;

